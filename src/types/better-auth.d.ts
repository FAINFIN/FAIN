/**
 * better-auth type stubs — no longer needed.
 *
 * The real .d.mts declaration files are present in
 * node_modules/better-auth/dist/ (installed from the npm tarball).
 * TypeScript resolves them via the package.json "exports" map with
 * moduleResolution:"bundler".  Keep this file empty so the ambient
 * module declarations here don't shadow the real package types.
 */
